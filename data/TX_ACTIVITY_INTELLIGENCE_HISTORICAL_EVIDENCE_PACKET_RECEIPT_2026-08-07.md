# TX Activity Intelligence — historical evidence packet receipt

**Packet:** `tx_activity_intelligence_historical_evidence_packet_2026-08-07.json`  
**Built:** 2026-08-07 PT  
**Scope:** Phase 2 hydration input for the already-frozen six-tab mockup only. This is historical, source-specific evidence; it is not a live activity event spine or a UI change.

## What is in the packet

| Frozen tab | Retained evidence included | Required state boundary |
|---|---|---|
| Daily activity | 869 accessible Fathom source-index records; 177 unique candidates / 189 account-call rows; 14 retained direct-reviewed calls; reported SFDC 90-day global aggregate | Fathom candidates and SFDC aggregate are `PARTIAL` / `CANDIDATE`; cross-source deduped activity is `UNKNOWN`. |
| Account timeline | 14 source-backed direct-review events and 4 selected Calendar strategy-visibility items | Events remain source-specific and `CANDIDATE`/`PARTIAL`; Calendar is not attendance or invite-origin proof. |
| Call ledger | 189 candidate account-call rows, each preserving recording locator, candidate match, recorder department and `NOT_CODED`; 14 review staging rows | `189` is rows, not unique calls. No semantic values are copied into metadata-only rows. |
| Plays & learnings | Frozen taxonomy rules, the 23-call separately scoped first-pass time mix, and four timestamp-linked comparator examples | eCom/config is not LIST; marketing is not SELL. Objection counts/dispositions remain `NOT_CODED`. |
| Coaching (private) | Candidate recorder-department queue and reviewed-call denominator | Person resolution and scorecards are `WITHHELD`; recorder is not owner/attendee/role. |
| Data coverage | S1–S9 source register plus explicit Jira/LIST source gaps and outcome-comparison block | Unknown remains visible; no zero substitution. |

## Validation performed

- Candidate ledger preserves **189** account-call rows and does not label it as the **177** unique-recording denominator.
- Reviewed ledger contains the retained **14** direct-review IDs; metadata-only unique candidate count is **163**.
- Selected timeline has **18** source-backed items: 14 direct-review events plus 4 Calendar evidence items.
- The scoped motion mix is stored only as its own **23-call / 19.2-hour / Jul 27–Aug 4** population.
- No event-level SFDC, Calendar, Jira, LIST, attendance, invite-origin, causal, per-person coaching or objection-disposition value was synthesized.
- All evidence objects carry source ID/path, period/as-of, locator where retained, state and a definition/non-claim.

## Renderer requirements

1. Preserve the frozen six tab names, order and privacy boundary.
2. Public call ledger renders `anonymous_call_key`, not raw recorder identity. `candidate_entity` needs the frozen UI's approved account-display treatment; it must retain `CANDIDATE` state.
3. Every rendered card/row must retain the packet’s source object and render state in its drill-down.
4. Do not sum source-specific activity or merge the 14/23/177/189 denominators.
5. Render `UNKNOWN`, `NOT_CODED`, `WITHHELD` and `COMPARISON_BLOCKED` literally—never as zero, a blank inference, or a favorable narrative.

## Excluded pending future evidence

- Event-level SFDC and Calendar adapters with canonical account/owner/attendee resolution.
- Jira export and account linkage.
- Canonical person → department/role map.
- LIST commercial-offer and leading-indicator metric contract/snapshots.
- Compatible outcome snapshots and a reviewed account-event join.
- Direct transcript review/coding for the remaining candidate corpus.

The packet is generated by `scripts/build_tx_activity_historical_packet.mjs`; rerun it after any retained source correction, then independently audit a source example before UI merge.
